import { MytimezonePipe } from './mytimezone.pipe';

describe('MytimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new MytimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
